README

```bash
conda metapackage \
--no-anaconda-upload \
-d 

```

```
Note: This error originates from the build backend, and is likely not a problem with poetry but with pyvcf (0.6.8) not supporting PEP 517 builds. You can verify this by running 'pip wheel --use-pep517 "pyvcf (==0.6.8)"'.
```